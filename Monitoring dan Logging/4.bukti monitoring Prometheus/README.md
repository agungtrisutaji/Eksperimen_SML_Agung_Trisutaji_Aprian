# Bukti Monitoring Prometheus

Tambahkan screenshot asli secara manual setelah Prometheus berjalan.

Screenshot yang disarankan:

- target FastAPI dalam status UP;
- query `model_requests_total`;
- query `model_prediction_latency_seconds`;
- query metrik model lain yang relevan.

Jangan membuat screenshot palsu.

